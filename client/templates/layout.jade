template(name="layout")
  core-header-panel
    core-toolbar
      paper-tabs(id="tabs", selected="meet", self-end)
        paper-tab(name="meet")
          | Search
        paper-tab(name="friends")
          | Friends
    +yield